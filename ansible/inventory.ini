; ansible/inventory.ini
;
; Static localhost inventory (P2) -- the host this playbook applies acts to
; is always the operator's local mac; there is no remote target and no
; role/matrix (Molecule was rejected as overkill for one host, one play --
; see the plan's Test Strategy).
[local]
localhost ansible_connection=local
