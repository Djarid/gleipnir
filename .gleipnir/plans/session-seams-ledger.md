# Superseded — see SESSION-STATE.md

This ledger is superseded by the single resume entry point
[`SESSION-STATE.md`](./SESSION-STATE.md), whose **Open seams** section absorbs
this list. See `SESSION-STATE.md` for current state + open threads.
(Kept as a tombstone to avoid a dangling reference; safe to delete.)
