[project]
name = "gleipnir"
version = "0.0.1"
description = "Gleipnir enforcement framework — reference implementation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
