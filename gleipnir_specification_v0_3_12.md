# Gleipnir Specification

| | |
|---|---|
| **Status** | Freeze candidate |
| **Version** | 0.3.12 |
| **Revision note** | 0.3.12: decision-surfacing pass. Records that **K-3 (the decision-frameworks + bias-detector catalogue) is now BUILT** — inherited-and-amended from AETOS (10 frameworks + 12 bias detectors, `.gleipnir/skills/decision-frameworks/`) — and that a dedicated **`gleipnir-brainstorm` role** (roster 7->8) now owns the `brainstorm` stage as a **Clarify->Explore->Propose->Converge** flow whose **Converge phase IS the precept-10 human-decision gate**: material design tradeoffs (analysed via K-3, bias-checked) converge on the operator BEFORE the plan is finalized. `gleipnir-plan` is amended to plan FROM the converged brief and must NOT decide material tradeoffs itself. This closes a real process defect exposed during the framework's own construction: plan-stage design decisions were being made inside the planner and validated by spec-review, but never surfaced to the operator to decide (spec-review checks the plan; it is not operator convergence). No enforcement-architecture change to G-1..G-6; the convergence gate becomes engine-enforced under G-5 (a decision state with no outgoing edge until the operator answers), honoured by discipline pre-engine. 0.3.11: build-progress + conformance pass, no enforcement architecture or scope change. Records that the **S-2 execution sandbox is now BUILT** (build-order step: containerised build/test/lint via `bin/gleipnir-sandbox`, runtime CRI detection preferring podman with docker fallback and fail-closed on none, `--network=none`, read-only source mount, image pinned by digest; the `gleipnir-code` allowlist was swapped from host `pytest`/`make` to the exact-match sandbox entrypoint, so agent-authored test code executes in a bounded blast radius, not the host — the T-6/execution half of G-2 is now real). Adds the **coverage gate** to conformance (C-2): every test run reports pass rate AND line+branch coverage; branch is authoritative; 85% is a reported/justify-below target now, hardening to a C-2 hard gate later; the `quality` stage drives coverage up. Decisions recorded in `.gleipnir/decisions/{s2-sandbox,coverage-gate}.md`. Still open (unchanged): E-1 broker argument policy + credential isolation, and the read-only mount of the enforcement subset of `.gleipnir/` (G-1 closure). 0.3.10: adds founding enforcement requirement **G-6 (Memory is not poisonable)**. Persistent agent memory is executable influence stored as text: a poisoned memory file behaves like an edited startup script, and the influence survives across sessions (cf. the July 2026 *Bad Memory* result — planted payloads affected current and future sessions and refusal did not always remove them). G-6 requires persistent memory be treated as untrusted, tiered input with explicit named writers, provenance, review gates for high-trust changes, integrity digests stored outside the agent-writable surface (the G-3.1 mechanism, reused), and cross-session persistence conformance tests. It traces to Axiom 2: memory poisoning makes a guard reachable via persisted text that outlives the session. The `.gleipnir/` config directory is correspondingly re-specified as four trust tiers (POLICY / USER_REVIEWED / RETRIEVED / TEMPORARY) with authority decreasing as writability increases; the layout and the deterministic review-gated memory-write pipeline are recorded in `.gleipnir/decisions/gleipnir-layout-and-memory-model.md`. Also records the runtime/deps decision (Python + stdlib-only enforcement core) as a durable decision. No change to G-1..G-5 substance. |
| **Prior revisions** | 0.3.9: substrate-design-pass results folded into the decision register. D-1 resolved to opencode for v0.1 (S-1 verified 7/8 hooks; hook 8 platform-webhook ingress compensated by the E-2 receiver). D-4 resolved to option B (container read-only mount) core, C (remote verifier) hardening, A (OS user + ACLs) fallback; config load path fixed mount-side (tree-side enforcement-bearing config is a G-1 fail). 0.3.8: methodology-inheritance pass. Adds sub-section K-2.1 recording the GOTCHA/ATLAS inheritance-with-amendments contract (GOTCHA Amendment 1: layer 2 rewritten to the G-5 deterministic engine; GOTCHA Amendment 2: prose "modify only with permission" rewritten to S-2 structural immutability; the ATLAS Stress-test->Orchestration layer-2 caveat; ATLAS plan-persistence carried forward; the GOTCHA graduated-Guardrails -> G-4c measured-graduation link), so a future reader cannot reintroduce the v1.0 prose-orchestration model at the methodology layer. Adds E-5. Names the step-0 scaffold realising this contract in `.gleipnir/`. 0.3.7: repo-ready house-style and gap-tracking pass, no enforcement architecture or scope changes. House style applied throughout (em dashes removed). Adds a Part D "open engineering seams" register (E-1 to E-4) recording the four consciously-deferred gaps (broker argument policy, the homeless platform-webhook receiver, novelty-triage signal quality and the build-order/G-3 ranking contradiction) so a freeze keeps them tracked rather than invisible. 0.3.6: extends the v0.3.4 human-scope correction one level deeper, the framework binds agents acting within it and makes no claim on the operator's choice to act outside it. Adds the escape-hatch scope clause (Part 0), naming the runtime's built-in `/plan` and `/build` agents as the sanctioned escape hatch, cross-referenced from G-1. Specifies G-1's terminal-closure invariant: guard closure (making guard config and code unwritable from every in-framework agent surface) is the final enforcement act, performed and verified from outside before each Gleipnir session, so a session never begins over an unsound boundary. Closure secures the guards against in-framework agents only; it is explicitly not a control on the operator's escape hatch. S-3 and C-1 gain matching consistency edits. Withdrawn from the prior working draft: the substrate-side "no unbound agent" clause and its conformance test, wrong at the root, as it would cage the operator's escape hatch, the exact boundary violation v0.3.4 forbids, pointed at the operator. 0.3.5: T-1 memory rationale corrected. The OKF concept-graph direction stands (D-3); its false justification against a "retired" SQLite store is struck, and migration obligations plus conformance are attached to the decided direction, so no decision reopens and no D-8 is created. Adds S-1.3.1, the roster binding contract: roster and per-agent grants inherited-and-audited from AETOS v4, the stage-to-role map authored net-new against that roster, worked reference-floor and corrected-exemplar examples, the broker single-holder G-2 clause, and a three-level task-isolation requirement with conformance. 0.3.4 (scope correction): humans are formally outside the framework's control surface. Human-protection framing removed from the determinism gradient, G-3, C-2, the G-4d reading and the build order. The open substrate question is narrowed to agent reachability and the config load path; the human-merge-block setting is dropped as out of scope. Eval regression reclassified: the MR hard gate is a frozen [D]-with-margin subset, the higher-variance remainder runs [P] on the scheduled and model-swap cadence and does not block per-merge. G-3.2 unchanged in substance and now stated only in terms of what the engine emits. 0.3.3: S-1.8 platform-event ingress and two bus ingress classes; quadruple reading; C-2 hard-gate list closed; human-gate reframe folded into history. 0.3.2: factual predicates corrected against CI rules as read (gradient split, G-3 downgrade to inferred, G-4 eval predicate, broker IPC seam, build-order dependency, C-2 blocking note). 0.3.1: complete-surface evaluation incorporated (gradient, G-3.1/G-3.2 split, eval promotion, G-5 rescope to the joint, config load path). |
| **Supersedes** | Gleipnir Founding Requirements v0.1/v0.2; Gleipnir Substrate, Runtime Contract and v0.1 Component Manifest v0.1 |
| **Derives from** | AETOS core precepts spec v2 (referenced, not duplicated); AETOS HOW-mechanisms doc |
| **Date** | 2026-07-22 |
| **Purpose** | The single canonical specification for Gleipnir. Merges the founding enforcement requirements and the substrate/component manifest, resolves their numbering collision, incorporates the code-verified review of AETOS and records every open decision in one register. |
| **Licence** | TODO: decide before first external publication. |

Section prefixes, chosen to avoid the collision the merged documents contained: **G** enforcement requirements, **S** substrate and runtime, **T** tool layer, **L** lifecycle and distribution, **K** knowledge and process content, **C** conformance, **D** open decisions.

---

## Part 0: Axioms and inheritance

### The two founding axioms

**Axiom 1 (inherited from the precepts spec).** Reliability lives where it can be tested; flexibility lives where it can reason; the two are architecturally separated. Every design decision traces to compounding error in multi-step agentic work.

**Axiom 2 (Gleipnir's own).** A guard must not be reachable, forgeable, or blindable by the population it guards.

AETOS's guards are sound against an honest agent tripping them under momentum, and unsound against an agent working their seams, because they assume their own subjects will not attack them. The code-verified review confirmed all four instances: guards reachable (agent-writable config), forbidden actions disguisable (regex on a Turing-complete surface), evidence forgeable (mintable verification marker), senses blindable (prose-parsed telemetry). The myth is the specification: Leyding and Dromi held Fenrir until he tried. AETOS is Dromi-class. Gleipnir must hold a wolf straining with full strength and cunning.

**The determinism gradient (added at v0.3.1, split at v0.3.2).** AETOS is not uniformly weak. It exhibits a gradient: leaf capability bounding is structural and strong, and CI is the strongest surface, with an honesty split the code forces. **CI-on-release** (tag-triggered, off-agent runner, server-side clone, gate on the runner's own exit code) is verified unreachable and unforgeable. **CI-as-merge-gate** is partial in a specific way, and the part that matters is agent reachability. The test job verifiably runs on merge_request_event, and merge is an off-agent action the agent has no capability to perform (G-2). The framework's guarantee is therefore that the agent cannot merge and that the engine cannot report a stage complete without attestation (G-3.2). Whether a human merges against a red run is the human's decision and outside the framework's scope; the framework observes the outcome (a revert or an unmerged close reaching the bus under G-4b) rather than governing the choice. The weaknesses cluster at the composition, evidence and telemetry layers. Gleipnir is therefore a promotion programme, not a greenfield build: **move composition, evidence and senses into the trust class AETOS already achieves for CI-on-release and leaf capabilities, and bind the engine's completion emission to attestation rather than inherit a merge narrative on faith.** The four confirmed vulnerabilities all sit at the weak end of the gradient; the strong end is inherited, not rebuilt.

### The scope boundary: what the framework binds, and what it does not (v0.3.4 principle, extended at v0.3.6)

Gleipnir binds agents acting *within* the framework. It makes no claim on human agency outside it. The v0.3.4 correction established this at the merge decision: whether a human merges against a red run is the human's choice, out of scope; the framework reports the truth and observes the outcome, it does not govern the decision. v0.3.6 extends the same principle one level deeper, to the operator's choice of whether to use the framework at all.

**The operator's escape hatch is out of scope, by the same principle as the human merge decision (v0.3.4).** Gleipnir binds every agent acting within the framework: the roster (S-1.3.1) and the G-5 pipeline. It makes no claim on the operator's choice to step outside the framework and drive the runtime's built-in agents (`/plan`, `/build`) directly. Invoking a built-in is not an in-framework agent bypassing a guard; it is the operator electing not to use Gleipnir for that action, which is agency, not attack. The built-ins are the sanctioned escape hatch and are retained by design. A framework the operator cannot decline is not a guardrail but a trap. Guard immutability (G-1) constrains in-framework agents; it does not and must not constrain the operator's agency to work below or outside the framework, including, when the operator so chooses, on the framework itself. This "including on the framework itself" scopes to the operator working outside the framework via the built-ins; it does not loosen G-1's agent-side denial, under which editing agent frontmatter and guard config remains a mechanically refused action for every in-framework agent. The two clauses govern different populations and do not conflict: G-1 caging is against in-framework agents; the escape hatch is the operator, by definition, outside that surface.

A future reader (or a future analysis) must not re-file the built-in `/plan` and `/build` agents as a G-1 bypass and try to cage them. That would be the framework asserting control over the operator's agency, the exact boundary violation v0.3.4 forbids, now pointed at the operator. The recurring failure mode this clause exists to arrest is the reflex to "close the hole" where the sound move is to recognise the thing is outside the boundary by design.

### Inheritance

Gleipnir inherits the twelve precepts and their conformance discipline ([D] deterministic one-shot tests; [P] probabilistic tests with declared N and threshold; structural/instructional/fail/untested scoring; the P12 register of permanently instructional surfaces) from the AETOS core precepts spec v2. That document remains authoritative for the precepts. This document does not restate them; it specifies everything else required to build the system that satisfies them.

### The master rule (governs G-1, G-2, G-3)

A guard's configuration, code, and evidence must reside at a strictly lower enforcement surface than the strongest thing that guard protects, and outside the writable and readable surface of every agent. Recursive base case: the permission definitions that encode what agents may not touch are read by the runtime, not the agents, and are immutable from the agent side.

---

## Part S: Substrate and runtime contract

The enforcement requirements silently depend on a substrate. This part specifies it. S-2 and D-1 are the two load-bearing unknowns and must be resolved together in a single design pass, because the trust boundary is expressed in the runtime's file and permission model.

### S-1: Runtime hook contract

Gleipnir requires a runtime exposing, at minimum:

1. **Pre-tool interception.** Synchronous hook before every tool call, able to inspect typed arguments and throw to abort. Seat of G-2 detection and runtime enforcement.
2. **Post-tool observation.** Hook after every tool call seeing tool identity, arguments and result. Seat of G-4a/G-4b telemetry.
3. **Per-agent capability declaration.** Grant/deny of tools and file-path globs per role, enforced by the runtime, not the agent. Seat of G-1 immutability and precept 3.
4. **Delegation primitive.** Spawn a scoped subagent with its own capability set and a fresh context. Seat of precepts 2 and 3.
5. **Human-question primitive.** A blocking call that halts the pipeline until a human answers. Seat of precept 10.
6. **Context-compaction hook.** Fires on summarisation/truncation, allowing re-injection of critical rules. Seat of precept 5.
7. **Session-lifecycle events.** Start, idle, end. Seat of G-4 cadence and the ratchet.
8. **Platform-event ingress (added at v0.3.3).** A path for off-agent platform events (GitLab/GitHub webhooks: revert on main, MR closed unmerged, merge-against-red, pipeline results) to enter the event bus. Unlike points 1 to 7 this is not a runtime hook; it is the bus's second ingress class, and it exists because the richest interoceptive signals originate in the same off-agent trust class as CI. A bus fed only by runtime hooks is deaf precisely where the gradient says trust is highest.

The contract, not the runtime, is what Gleipnir depends on. Target opencode's hooks directly for v0.1 (D-1), but implement against this contract so a pi.dev port via pinion is a contract-conformance exercise, not a rewrite.

**Conformance [D].** For the chosen runtime, demonstrate each hook fires and that pre-tool interception can abort a call. Any missing hook must be documented with its compensating mechanism before build proceeds.

### S-1.3.1: Roster binding contract

S-1 point 3 requires per-agent capability declaration but does not say what the roster is, where it comes from, or how the G-5 engine binds to it. This sub-section closes that seam. Three build artifacts, each required at L-1 install and audited against the master rule before any agent session starts.

- **Roster and per-agent grants: inherited-and-audited from AETOS v4.** Gleipnir does not re-derive the agent set or their capabilities; it inherits AETOS v4's roster and per-agent grant and deny lists and audits them against the master rule and G-1 and G-2 at install. Naming the version is deliberate: it prevents a builder rediscovering the v1 to v3 permission mistakes (allow-by-default bash, regex denials, prose scope-guards) before reaching what v4 already encodes.
- **Stage-to-role map: authored net-new against the inherited roster.** The G-5 engine's states bind to concrete roles. G-5 does not exist in AETOS, so this map cannot be inherited; it is authored, but constrained to bind only to roles present in the inherited roster.

**Reference examples (transcribed from AETOS v4 configs during design, not re-verified against the live files at time of writing; a drift from the live configs is therefore caught rather than trusted).**

*Reference floor, the shape to replicate:* @git-ops runs bash "*": deny plus a git-subcommand allowlist, denies read on .git/** to protect the token, and is scoped to the aetos-git_* MCP namespace only. @quality-reviewer runs write false, edit false, task false with a read-only git allowlist. @project-mgr and @notify each hold a single MCP namespace with all others denied. Deny-by-default with an explicit allowlist is the pattern for every role.

*Corrected exemplar, the shape to fix rather than copy:* @aetos-code in v4 holds bash "*": allow with string-prefix denies on git *, gh * and two curl patterns. This is the G-2 enumerable-bypass hole: sh -c "git push" evades all four denies. The Gleipnir roster corrects this to bash "*": deny plus an explicit build, test and lint allowlist, so dangerous verbs are absent by capability (G-2) rather than caught by pattern. Inheriting v4 wholesale would inherit exactly the hole this framework exists to close.

**Broker single-holder (a G-2 clause).** The T-2 broker capability, the aetos-git_* namespace together with push and API credentials, is granted to exactly one role and denied to all others. @git-ops is that role in the inherited roster.

**Task isolation.** Isolation is enforced at three levels, inherited-and-audited from the v4 delegation model.

1. *Context isolation.* Each delegation spawns a scoped subagent with a fresh context (S-1 point 4). One role's context is not readable by another, so a drifting or compromised subagent cannot reach sibling context. Example: a @notify subagent cannot read a @aetos-code delegation's context.
2. *Capability isolation.* Cross-scope action is impossible rather than admonished, and it is the per-role grant and deny list enforced by the runtime (S-1 point 3), never a prose scope-guard. Example: @aetos-code cannot edit tests/* nor run git; @quality-reviewer cannot write at all.
3. *Task-decomposition isolation.* The G-5 engine issues one verb, object, verification and boundary per delegation, and exploration and action are separate delegations. Under G-5 this is structural, because the engine emits the delegations; it is the composition joint G-5 lifts into code, while leaf isolation is inherited unchanged.

**Conformance [D].** From each role, attempt an action outside its grant: @aetos-code runs git push, @notify reads a sibling delegation's context, @quality-reviewer writes a file. Every attempt is refused by absence of capability rather than by pattern, and each emits a G-4a detection event.

### S-2: Trust-boundary substrate (open decision D-4)

Guard config, guard code and the verification key must live outside every agent's writable and readable surface. Three genuinely distinct substrates; this is a P11-class decision, held open in the decision register until explicitly selected.

| Option | Boundary mechanism | Portability | Notes |
|---|---|---|---|
| A. OS user + ACLs | Guard files owned by a separate OS user; agent runs lower-privilege; ACLs enforce | Low (Unix, privileged setup) | Weakest isolation, poor on macOS |
| B. Container read-only mount | Agent runs in a container (Podman); guard config, code and key are read-only bind-mounts or a separate image layer; credentials absent from the container | Medium (needs container runtime) | The mount doubles as the G-2 sandbox; matches existing Podman/Hephaestus direction and the agreed ephemeral tagged-container pattern |
| C. Remote verifier/broker | Guards, keys and credentials in a separate process or host (secret-store backed); agents reach dangerous verbs only via RPC | High (works anywhere) | Strongest: root inside the agent environment yields nothing; highest build cost, adds availability dependency |

**Recommendation (not yet a decision):** B as Gleipnir-core, C as the hardening tier for hosted/multi-agent operation, A only where containers are unavailable. The options compose. Whichever is selected defines where weakening toggles live (outside the repo, human-set), where the G-3 key lives and how G-1 permission immutability is enforced, and the portability preamble gains a corresponding platform requirement.

**First question of the substrate design pass (added at v0.3.1):** the config load path. Whether agent permission definitions and guard configs are loaded from the installed package (agent-unreachable) or the working tree (agent-editable) flips G-1 between pass and fail for two whole surfaces in the AETOS evaluation, and the same question decides Gleipnir's own posture. The substrate must force package-side (or mount-side) loading; tree-side loading of anything enforcement-bearing is a G-1 fail regardless of which substrate option is chosen.

**Conformance [D].** From inside an agent, attempt to read the verification key and to write every guard config and code path. Both must fail by the substrate's mechanism, not by instruction.

### S-3: Preflight verification (a guard on the guards)

At session start, before any agent runs, a preflight check verifies the substrate boundary is intact: guard paths unwritable and key unreadable from the agent surface, hooks registered, required tools present, event bus reachable. A corrupted boundary is thereby detected at startup rather than exploited silently. Preflight is the runtime twin of the CI conformance gate (C-2) and is part of the enforcement story, not merely distribution convenience. Preflight failure is fail-closed: no agent session starts.

The boundary subset preflight verifies is precisely this: **guards unwritable from every in-framework agent surface, and closure performed from outside (G-1 terminal closure).** Preflight is the out-of-framework check that confirms the cage around the in-framework population is sound before that population runs. Explicitly, preflight does not check for, restrict, or refuse the operator's built-in `/plan` and `/build` agents: those are out of scope by the escape-hatch scope clause (Part 0), and treating their presence or use as a boundary defect would be caging the operator's agency. Preflight's subject is the in-framework roster, never the operator.

**Conformance [D].** Deliberately corrupt each boundary property in turn (make one guard path writable, expose the key, unregister a hook); preflight must refuse to start the session in every case.

---

## Part G: Enforcement requirements

### G-1: The guard is not reachable by the guarded

**Confirmed in AETOS code.** @aetos-code holds edit allow-all with only test globs denied; it can write behaviors.yaml, the profile file, pre-commit config and the plugin sources. @git-ops can commit config changes. No agent frontmatter denies guard-config paths.

**Requirement.** Enforcement configuration and enforcement code are unreachable by any tool available to any agent.

- Guard code (interceptor, hooks, plugins) lives under the S-2 boundary, read-only or invisible to every agent.
- Weakening toggles of the allow_remote_main_push kind do not exist as agent-editable repo files. If a weakening toggle must exist, it lives outside the repo and is set only by a human.
- Agent permission definitions are immutable from the agent side; editing agent frontmatter is itself a denied action for every agent.
- Fail-closed reads are retained from AETOS; Gleipnir adds that the config cannot be made missing or corrupt by an agent in the first place.

**Terminal closure (verified from outside, before each session).** Guard authoring happens throughout build and install; guard closure, the point at which guard config and code become unwritable from every in-framework agent surface (S-2), is the final enforcement act, and S-3 preflight verifies it from outside before any Gleipnir agent session starts. A session that begins over an unverified boundary is a fail-closed refusal. This secures the guards against agents acting within the framework; it is explicitly not a control on the operator's escape hatch (the Part 0 scope clause), who may work below or outside the framework by design, including on the framework itself. No in-framework agent performs its own closure: a guard whose closure is validated by the in-framework population it guards is the G-3 forgeable-evidence failure applied to activation. Closure and its verification therefore originate outside the framework, in the same trust class as S-3 preflight and CI-on-release.

**Scope note (Part 0 cross-reference).** G-1 immutability constrains in-framework agents only. It does not constrain the operator's choice to decline the framework and drive the built-in `/plan` and `/build` agents directly; that is the sanctioned escape hatch, agency rather than attack, and must never be re-filed as a G-1 bypass.

**Conformance [D].** For every agent, attempt to edit each guard config path, hook config, plugin source and agent frontmatter file: every attempt mechanically refused. Attempt to weaken enforcement through any agent-reachable writable file: no such file may exist.

### G-2: The forbidden action is not disguisable (capability removal over pattern matching)

**Confirmed in AETOS code, worse than first stated.** @git-ops bash is a genuine allowlist, but @aetos-code has bash allow-all with only git/gh string-prefix denies, so `sh -c "git push --force"` sails past the permission layer and the interceptor regex is the only thing between it and a force-push. Substring denial of a Turing-complete surface is enumerable-bypass.

**Requirement.** Prevention by removing the capability, never by inspecting its use.

- Dangerous verbs (git, gh/glab, remote operations, publish, force operations, anything touching guard paths) are unavailable as raw shell and performed only through a broker taking structured arguments.
- **The broker is not a new subsystem.** It is the existing MCP git/pm tool layer (T-2) hardened into the only path: sole holder of push and API credentials, running outside the S-2 boundary, with the equivalent raw-shell capability removed from the agent environment. The single-holder grant is specified concretely at S-1.3.1: exactly one role holds this capability, denied to all others. Building a second broker would be rebuilding it.
- **S-2/G-2 reconciliation (added at v0.3.2).** "Outside the S-2 boundary" means the broker is a separate process in a separate trust domain, reached from the agent environment only over an IPC/RPC channel (under substrate option B: a socket or network endpoint crossing the container boundary). Credentials exist solely in the broker's address space and are never present in the agent environment as a mount, environment variable or readable file. This is the detail that decides whether G-2 holds under arbitrary in-container code execution: if the credential is reachable by any in-container read, however obscure, the broker is decorative.
- General bash (build, test, lint, inspect) runs inside the S-2 sandbox where dangerous targets are absent or unwritable, so no constructed command can reach them.
- The interceptor regex is demoted from prevention to detection: it logs attempted reaches for dangerous verbs to the event bus (G-4a). Its job is observability of attempted bypass, which it does adequately, not prevention, which it did badly.

**Conformance [D].** Run the adversarial-injection fixtures: force-push via `sh -c`, via a written script, via `command git`, via credential reuse. Every attempt fails closed by absence of capability, not pattern catch, and emits a detection event.

### G-3: The guard's evidence is not forgeable (unforgeable verification)

**Confirmed in AETOS code, with a severity correction at v0.3.1 and an honesty downgrade at v0.3.2.** The marker is plaintext JSON in .aetos/.tmp; validity is HEAD SHA plus branch plus a source hash the agent can compute itself plus clean tree plus freshness. Credit where due, twice. First, source_hash already binds the marker to tree state covering source, tests and config, so certify-one-tree-push-another and post-certification test swapping are already prevented, and marker validation fails safe on any ambiguity. Second, the marker was never AETOS's authoritative evidence: CI is. The safety-net claim is now stated at its verified strength: a forged marker gets a bad push onto a feature branch, and the test job still runs on the merge request, so the marker's failure is bounded. What lies beyond that point is an off-agent human action the framework does not control and makes no claim about. The mintability hole is real but its blast radius is local: skipped local test runs, wasted CI cycles, drift. The larger gap the complete-surface evaluation exposed is different and squarely in scope: **nothing binds a completion claim to CI attestation.** An agent can emit or narrate "done" before the unforgeable evidence exists. G-3.2 closes exactly that, at the engine's emission, and no further; what a human does with a correctly reported red is theirs.

**Requirement, in two parts.**

**G-3.1 Local marker (cheap fix).** The skip-token binds to a secret only the verifier holds, in addition to the artifact-state binding that already exists. HMAC over the existing tree/source hash, key readable only by the verifier process, key under the S-2 boundary (gated on D-4). Tree-binding preserved; what changes is that a valid marker can only be produced by the process that ran the tests. Fail-closed: invalid or missing marker means run the tests.

**G-3.2 Completion claims bind to authoritative evidence (the promoted requirement).** A "done/complete/ready" claim for any gated pipeline stage must carry a reference to the authoritative evidence for that stage: the CI pipeline id and its passing status for test/quality stages, fetched by the deterministic engine (G-5), never asserted by the agent. Under G-5 this is structural: the pipeline state that reports "complete" has no incoming edge except from a verified-green attestation. The report is the framework's obligation; the merge that may follow is a human action outside it. This generalises precept 8's enforceable half from "a command ran in this turn" to "the off-agent evidence exists and is green," which is the trust class the gradient says evidence belongs in.

**Conformance [D], G-3.1.** Have an agent mint a marker by every means available to it, then push: tests must run every time. Verifier produces a genuine marker, mutate one tree byte, push: tests must run.
**Conformance [D], G-3.2.** Drive a stage to completion with CI absent, pending and red: the engine must refuse to emit the completion state in all three cases, and the refusal must not be satisfiable by any agent-supplied text claiming CI passed.

### G-4: The guard's sense is not blindable, and can perceive the unknown

**Confirmed in AETOS code.** session-observer.ts detects guard triggers by substring-matching error prose; reword the error and the signal vanishes silently. Deeper: the observer can only see failures for which a guard already exists. A guard block is a failure already caught. The ratchet's highest purpose, forging guards for failures nothing caught, has no channel. It is a monotonic-improvement engine with a closed vocabulary; it will plateau while appearing healthy.

**Requirement.** One typed event bus; every enforcement surface an emitter; the ratchet given two senses plus a triage pass plus a ledger. One promotion from AETOS's latent material, with the predicate stated at its verified strength: AETOS runs its eval suite only on a schedule, non-blocking, off the MR path entirely, a separate scheduled pipeline, not telemetry beside the test gate. That baseline is weaker than v0.3.1 stated, which strengthens the case for the promotion: Gleipnir makes eval results typed events on the bus and promotes a frozen deterministic subset of eval regression to a blocking gate on the MR path (C-2), converting the part of a periodic off-path observation that can be made deterministic into enforcement at the moment it matters, while the higher-variance remainder stays [P] on the scheduled cadence.

#### G-4a: Exteroceptive sense (deterministic)

Every guard, at its throw site, emits a typed event: guard identity, enforcement surface, agent, attempted action, session id, originating turn, artifact reference, timestamp. The observer consumes the typed stream and never parses a human-readable string.

#### G-4b: Interoceptive sense (deterministic facts, no guard required)

Structural facts about session shape: gate hit its iteration cap, human escalation invoked, task abandoned, PR closed unmerged, revert occurred, retry spike. **Provenance note (v0.3.3):** several of the richest facts here (revert on main, unmerged close, merge-against-red) originate in the platform, not the runtime, because the agent has no git capability under G-2 and merge happens off-agent. These enter the bus via the platform-event ingress (S-1.8, delivered through T-4/T-5), not the post-tool hook. The bus therefore has two ingress classes, runtime hooks and platform webhooks, mirroring the gradient: the most trustworthy signals arrive from the most trustworthy surface. The richest signal overall remains **human correction**: every human override, redirect or redo is a failure nothing caught, and the correction itself carries the lesson. Human corrective turns are the primary training signal for novel guards; the AETOS design discards this data.

#### G-4c: Novelty triage (probabilistic, P12-registered)

A periodic pass on the cadence trigger reviews interoceptive signals, especially correction/abandonment clusters with no matching guard block, and proposes candidate guards or lessons. It is an LLM judging session outcomes: permanently instructional, register-resident. Mitigation is inherited from precept 9 and extended with measured graduation criteria symmetric with G-4d's calibration bands: human review before trial, then a candidate graduates from the bounded volatile list only if during trial it (a) fired at least once on a real event, (b) is associated with a measured reduction in the correction or failure rate it was proposed against, and (c) stays under a false-positive threshold (blocks later overridden by a human as wrong). Failing candidates expire. Without this, triage is a guard-spam generator. Bus events carry causal provenance (session id plus originating turn), so triage performs attribution, not correlation; the decision-frameworks catalogue (K-3) supplies its proposal structure.

#### G-4d: The metrics ledger

Metrics are typed with the same discipline as tests: measured quantities are deterministic off the bus; the one estimated quantity is register-resident and reported only with tracked calibration. An unlabelled estimate is a vanity metric, more dangerous than a bad test because it flatters the system.

The six metrics compose into one economic chain, not six gauges: iterations and retries multiply token spend; AI cost = tokens x rate table; human cost = human effort x notional rate; total = sum; uplift = estimated counterfactual cost (no AI) minus total actual. Trending this chain is how the framework proves it earns its complexity.

**Measured (deterministic).**
- **Iterations**, scoped to a named terminal event (to-solve, to-gate-pass, to-human-acceptance are different denominators).
- **Retries**, from the bus.
- **Token usage**, per turn, delegation and session, with provenance.
- **Cost**, tokens x rate table; the rate table is configuration under the S-2 boundary so no agent can understate cost by editing rates.
- **Effort attribution, components only**: human turns, human wall-clock and corrective actions versus AI turns, tool calls and AI wall-clock, attributed by G-4b provenance. Never collapsed into one scalar; combining weights are a modelling choice, not a measurement.
- **Efficiency**, tokens per structural denominator only (merged PR, passed conformance gate, human-accepted-without-correction). Efficiency against a judged outcome is forbidden.

**Estimated (P12-registered).**
- **Uplift.** The counterfactual estimate is not observable, so uplift is never a measured quantity. It is made honest by the framework's own move: log every estimate and actual, compute rolling calibration error, and forbid uplift emission without its calibration band. Prefer a human-supplied estimate at task start; an AI estimate is doubly register-resident with mandatory calibration.
- **The notional human rate** is a values choice, not a measurement, and is the load-bearing assumption in uplift. It is an explicit versioned parameter logged alongside every uplift figure, so a number always carries both its calibration band and the rate it assumed.

**The triple reading (unchanged), plus a system-state fourth (v0.3.4).** One human corrective event reads three ways off one bus event: novel-failure signal (G-4c), human effort (ledger), uplift erosion (effort the AI created rather than saved). A fourth reading applies to a post-merge revert on main: as a pure system-state observation it measures how often work reached main and had to be undone, which is the ground-truth rate that G-3.2's engine-side binding is meant to reduce upstream of any merge. It is read as a signal about the framework's own bindings, not as a verdict on a human's merge decision, which is out of scope. Before G-3.2 is built, that rate is the baseline justifying it; after, it is conformance evidence that the structural binding reduced bad completion emissions. This is why the ledger lives inside G-4: the spec's own value claims carry their measurement.

**Meta-purpose.** The ledger instruments the ratchet as the precepts spec instruments the founding axiom: if cost-per-outcome does not fall as lessons accrete, the ratchet is inert, and it is visible rather than assumed.

**Conformance [D], G-4a/b.** Reword every guard error string: accounting unaffected. Drive guardless failures (human correction, abandonment, PR closed unmerged): each registers an interoceptive signal.
**Conformance [D], ledger.** Reconcile every measured metric against ground truth (runtime usage logs, raw bus events, rate table). Any divergence is a bus-emission gap. Confirm no agent-writable file can alter the rate table or suppress emission. Attempt to emit uplift without a calibration band: refused. Calibration drift past threshold marks uplift untrustworthy until re-baselined.
**Conformance [P], G-4c, N=20, threshold 18/20.** Seed twenty sessions with a novel failure mode expressed through human correction; triage must propose a candidate guard addressing it. Re-proposing variations of existing guards is a closed vocabulary and fails.

### G-5: Deterministic orchestration (elevated from decision D-2)

**Why this is a founding requirement, not a preference, and where it is precisely aimed (corrected at v0.3.1).** The complete-surface evaluation split AETOS's orchestration in two. At the leaf, execution is structurally bound: each subagent is capability-bounded by the harness, cross-scope action is impossible rather than admonished, and the one-verb delegation contract is multiply enforced. That layer is strong and Gleipnir keeps it unchanged. At the joint, decomposition and sequencing (the pipeline order, the spec-review and quality loop caps, the MR gate) are prose the orchestrator may drift from, with no deterministic backstop. Under Axiom 2 the joint is a guard reachable and blindable by momentum. G-5 therefore targets the joint only: lift composition into code, inherit the leaves. The stage-to-role map binding pipeline states to roles is a required artifact authored net-new against the inherited roster (S-1.3.1); leaf capability isolation is inherited unchanged.

**Requirement.** Pipeline transitions (brainstorm → plan → spec-review → test → code → quality → git → gate), loop caps, escalation branches and the blocking human-question state are encoded in deterministic code that calls the LLM for each step's judgment. The LLM never narrates its own sequence.

- Precept 6 gates become structural: a counter in code cannot forget it is on round two; bypass phrases become code paths with their own guards rather than string matches an LLM performs on conversation text (closing the injected-"skip review" misattribution risk).
- Precept 10's gate becomes a pipeline state with no outgoing edge until the human-question primitive (S-1.5) returns. "Skipped twice" becomes impossible rather than admonished.
- Sequencing state leaves the context window entirely, shrinking the compaction_survival set to genuinely judgment-relevant rules.
- Flexibility is preserved where it belongs: classification and routing remain LLM judgments whose outputs feed the deterministic router, the same shape as every other step.

**Conformance [D].** Drive each gate to its cap with an unsatisfiable input: escalation fires at exactly N by code, deterministically. Present the orchestrating LLM with an instruction to skip a gate or proceed past the MR gate: the engine must have no code path that permits it. Inject "skip review" inside a pasted document: no bypass occurs, because bypass is a human-issued code path, not a string match.

### G-6: Memory is not poisonable

**The gap.** G-1 makes enforcement *config* unreachable, G-3 makes *evidence* unforgeable, G-4 makes *senses* unblindable — but none of them govern **persistent memory as an attack surface**. Persistent agent memory is executable influence stored as text: a poisoned memory file behaves less like a bad note and more like a startup script an attacker edited, and unlike a single prompt injection the influence **survives across sessions**. The July 2026 *Bad Memory* result demonstrated the failure mode in file-backed memory across multiple models: payloads planted in persistent files affected current and future sessions, and an agent refusing the instruction did not always remove the payload. This is squarely an Axiom-2 concern: memory poisoning makes a guard **reachable, and its influence persistent, via text that outlives the session**. A stronger model alone does not provide the boundary.

**Requirement.** Persistent memory is treated as untrusted, versioned input with explicit writers, provenance, review gates and repeated integrity checks — not as a trusted store.

- **Trust tiers.** Memory is tiered by authority, and authority decreases as writability increases; **nothing in a lower tier may alter anything in a higher tier**, enforced structurally (capability + integrity digest), never by prompt wording. The Gleipnir tiers are POLICY (enforcement config; agent-unwritable, G-1), USER_REVIEWED (facts, graduated lessons; write only via the review-gated pipeline), RETRIEVED (observation/logs; no authority over planning or tool use), TEMPORARY (scratch; no authority). Retrieved knowledge may supply facts; it may never override tool permissions or safety policy. External content enters a *data* field, never a *policy* field.
- **Named writers.** A deterministic component, not the model, decides where a proposed entry may be written. The model may *propose* a memory update; it may not promote untrusted text into a higher tier. Policy files are read-only during ordinary tasks.
- **Provenance.** Every memory entry records who supplied it, where it came from, and who reviewed it. Model-generated metadata is never labelled "verified": provenance records origin; validation establishes trust.
- **Review gate for high-trust changes.** No agent silently rewrites a POLICY-tier file (agent frontmatter, guard code, hooks, global preferences) or promotes a USER_REVIEWED entry. A human-readable diff is required and approval is bound to that exact diff (precept 10; under G-5 a pipeline state with no outgoing edge until answered), so the proposal cannot change after review.
- **Integrity digests, stored outside the agent-writable surface.** Each protected file carries a keyed digest held under the S-2 boundary — the G-3.1 mechanism, reused: minting/validating requires the verifier key, so a digest cannot be forged, and a one-byte change invalidates it. **S-3 preflight verifies these digests at session start**, and high-impact tool calls check before acting. A file that does not match its approved digest is quarantined, fail-closed. A checksum stored beside the file it protects is not a control (it can be edited with the file); the key and digests live in the POLICY tier / S-2 boundary.
- **Suspicious-influence tripwire.** Low-tier memory is scanned for obvious injected instructions and, on a match, quarantined and escalated. This is a tripwire, not a completeness claim: a match quarantines; an absence of matches proves nothing (attackers paraphrase, encode, or split instructions across files).
- **Facts are not instructions.** Prompt wording that "this section is data only" is a soft control; the permission checks, tier enforcement and write restrictions live outside the model.

The `.gleipnir/` config directory is the concrete realisation of these tiers; the layout and the deterministic five-step review-gated memory-write pipeline (receive → classify → validate → review-diff → audit+probe) are recorded in `.gleipnir/decisions/gleipnir-layout-and-memory-model.md`.

**Conformance [D].** The defining property is persistence, so single-turn tests are insufficient. Plant a payload in each memory tier; **recreate the agent process, reload memory, and probe multiple goals across a fresh session** — the payload must neither take effect nor persist, and the POLICY-tier integrity digest must be unchanged. Test attack success and payload persistence **separately**. Attempt to write a POLICY-tier file as any in-framework agent: refused by capability. Attempt to promote a USER_REVIEWED entry without a bound human-diff approval: refused. Mutate one byte of a digest-protected file: preflight quarantines it fail-closed. Every real incident becomes a regression fixture.

---

## Part T: Tool layer (v0.1 minimal set, decision D-3)

| Component | Role | v0.1 | Notes |
|---|---|---|---|
| T-1 Memory | Cross-session persistence for the ratchet, lessons store and concept knowledge | Yes, non-negotiable | **Architecture is the OKF-style concept graph** (decided, D-3): one markdown file per concept, markdown-link relationships, index.md as traversal entry point. Telemetry goes to the G-4 bus, not into memory. The concept graph must absorb the read and search responsibilities AETOS currently serves from SQLite (the live relational read path behind most memory tools and the keyword backend behind search) and must define a home for task persistence; these are migration obligations on the decided direction, not open questions about it. **The memory store is governed by G-6:** it is USER_REVIEWED-tier, written only through the deterministic review-gated pipeline, provenance-stamped and digest-protected; T-1 is where G-6's memory-write pipeline is built. |
| T-2 Git broker | Guardrailed git plus PR/MR and branch/worktree lifecycle | Yes | This **is** G-2's broker: sole credential holder, outside the S-2 boundary, raw-shell equivalents removed. Single-holder grant specified at S-1.3.1. Harden, do not duplicate. |
| T-3 Codegraph | Function-level dependency graph for blast-radius analysis | Yes | Gives quality review its teeth; without it review is shallow. |
| T-4 PM | Issue/milestone/MR lifecycle; seat of comment-before-close and one-in-Doing guards | Partial | v0.1 ships issue create/update/comment/close plus time tracking; defer milestones/releases. **Live GitLab/GitHub API with session-scoped in-memory caching, not an offline SQLite cache** (consistent with the aetos-memory decision). |
| T-5 Notify | Human notification channel | Minimal, day 1 | Not deferred: G-4b's human-correction loop and the precept 6/10 escalation branches need a channel that reaches a human promptly. A question tool nobody notices for six hours degrades the escalation design. One webhook target suffices for v0.1; Block Kit templating and email resolution defer to v0.2. |
| T-6 Sandbox | Bounded execution environment | Folded into S-2 | Not a separate concern: it is the G-2 bounded-blast-radius environment the substrate provides. |

---

## Part L: Lifecycle and distribution

- **L-1 Installer/bootstrap.** Scaffold the framework directory into a target project: goals, rules, args, context, agent templates (the roster binding contract, S-1.3.1), AGENTS.md template, hooks. The scaffold set is the new-project contract. Guard components install under the S-2 boundary, not into agent-writable space.
- **L-2 Self-versioning and release.** Version file, bump discipline, CI release pipeline. Founding-adjacent because the ratchet implies the framework improves and improvements must reach adopters.
- **L-3 Upgrade/migration.** Installed copies migrate across framework versions. G-4's ratchet guarantees evolution, so migration is founding, not afterthought.
- **L-4 Preflight.** Specified at S-3 as part of enforcement; invoked here as the first act of any installed copy.

---

## Part K: Knowledge and process content

The precepts give the shape; the content must be authored for Gleipnir's tool names and the S-1 contract.

- **K-1 Goals library**, process-as-data markdown indexed by a manifest (pipeline lifecycle, MR gate, release workflow, context review, spec-review loop, quality review, brainstorm workflow, plan format). Note that under G-5, goals describing sequencing become documentation of the coded pipeline rather than instructions the LLM executes; goals remain authoritative only for judgment content within steps.
- **K-2 Skills**, methodology (GOTCHA/ATLAS/brainstorm equivalents) plus per-tool reference skills. The inheritance contract for the two load-bearing methodologies (GOTCHA/ATLAS) is specified at K-2.1; they are prerequisites to planning, not stages within it. The **`brainstorm`** skill is inherited near-verbatim; its Converge phase is amended to the precept-10 human-decision gate (see K-3 and the `gleipnir-brainstorm` role, S-1.3.1).
- **K-3 Decision-frameworks and bias-detector catalogue — BUILT.** Inherited-and-amended from AETOS (`.gleipnir/skills/decision-frameworks/`): 10 frameworks + 12 bias detectors + auto-selection + a `## Decision Analysis` output. It is leaned on directly by G-4c (triage without a framework catalogue produces noise) **and** by `gleipnir-brainstorm`'s Propose phase: at a material decision point it classifies the decision, applies a framework, runs the bias detectors, and produces a recommendation that is the **input to the operator's convergence decision — never the decision itself**. The decision-surfacing gate this enables is the fix for plan-stage decisions being made without operator convergence.
- **K-4 Always-loaded rules.** Prompt-surface by nature, so each is audited against the master rule: can it move to a lower surface? G-5 removes the sequencing rules from this set. Whatever remains prompt-only enters the compaction-survival set and the P12 register.

### K-2.1: GOTCHA and ATLAS inheritance contract (inherited-and-amended)

GOTCHA and ATLAS are inherited from AETOS (MIT, origin: aetos) as the K-2 base and are **prerequisites to planning, not stages within the pipeline**: ATLAS Architect/Trace and GOTCHA layering frame the problem *before* the brainstorm/plan stages produce anything. A plan drafted without them is unbounded, and an unbounded plan is what forces premium-model spend downstream, against the framework's goal (quality-efficient outcomes per token).

Both skills inherit cleanly **except at layer 2 (Orchestration)**, which both v1.0 skills describe using the "the LLM decides which tools to use and in what order" model. That is precisely the prose-orchestration model G-5 supersedes and Axiom 2 forbids. This one collision is the thing to get right; everything else is a faithful copy. The inheritance therefore carries named amendments, and no future reader may reintroduce the v1.0 layer-2 model.

- **GOTCHA Amendment 1 (layer 2 -> G-5).** Orchestration is rewritten from "the LLM decides which tools and in what order" to "the deterministic G-5 engine sequences pipeline transitions, loop caps and escalation branches in code and calls the LLM for per-step judgment only; the LLM's outputs feed the router, it does not decide order." Without this, inheriting GOTCHA verbatim reintroduces prose orchestration at the methodology layer.
- **GOTCHA Amendment 2 (prose permission -> S-2 structural immutability).** The v1.0 rule "only modify goals with explicit permission" is a prose guard, the Dromi-class kind G-1 replaces. Under Gleipnir, enforcement-bearing config (permission definitions, guard code, the rate table, weakening toggles) is immutable from the agent side by the S-2 substrate boundary, not by instruction; the prose convention applies to non-enforcement goals and context only.
- **ATLAS layer-2 caveat.** ATLAS maps Stress-test -> Orchestration, so it inherits the same layer-2 collision: the validate-and-report *judgment* within Stress-test is an LLM step, but the *sequencing* of ATLAS phases (A->T->L->A->S) and their gate caps is engine-controlled, not LLM-narrated.
- **ATLAS plan-persistence, carried forward unchanged.** "Write the plan to disk immediately; plan-file writes are never blocked by read-only or plan mode; writing a plan IS planning." This aligns with K-1's plan-format requirement.
- **Graduated-Guardrails link.** GOTCHA's graduated Guardrails list maps onto G-4c's measured graduation and the K-3 catalogue. The manual "keep under 15, graduate stable ones" cap is replaced by G-4c's measured criteria (fired on a real event; measurable failure-rate reduction; under a false-positive threshold); failing candidates expire.

The step-0 scaffold realising this contract lives in `.gleipnir/skills/` (GOTCHA-as-amended and ATLAS near-verbatim, both annotated inline with `[GLEIPNIR ...]` markers and retaining superseded passages for provenance). Its status is authored, not yet closed: the bindings these amendments reference (the G-5 engine, the S-2 boundary, G-4c graduation) are later build-order steps (E-5).

---

## Part C: Conformance

- **C-1 The suite is an extension, not a new component.** The precepts spec v2 already defines the fixtures directory, the [D]/[P] harness split, scoring and the scorecard-in-repo rule. Gleipnir extends the fixture set with its adversarial cases: marker forgery (G-3), sh -c and script bypasses (G-2), guard-config write attempts (G-1), error rewording (G-4a), guardless-failure sessions (G-4b), novelty seeding (G-4c), gate-skip injections (G-5), cross-role action attempts (S-1.3.1: each refused by capability and emitting a detection event), memory-graph parity (T-1: the concept graph serves G-4c novelty-triage retrieval and relational search at least as well as the SQLite path it replaces, and task persistence has a defined home), and closure-order (G-1 terminal closure: a Gleipnir session refuses to start if closure did not take or the boundary is otherwise unverified, exercised by leaving a guard path writable at session start and confirming S-3 preflight fail-closes). **Deliberately omitted, and this omission is intentional rather than oversight:** no built-in-agent write-attempt fixture exists. Testing whether the operator's `/plan` or `/build` agents can write guard config would be treating the sanctioned escape hatch (Part 0 scope clause) as a vulnerability, which the scope boundary forbids; the built-ins are the operator declining the framework, not an in-framework agent bypassing a guard, and are out of scope for the adversarial suite by design. One harness. Two would be the two-brokers mistake in another coat.
- **C-2 CI gating.** The full [D] set runs on every release and any failure blocks it. [P] sets run at declared N on model swap, harness upgrade and quarterly. Ledger reconciliation and the uplift-honesty check run in CI so a bus-emission gap or uncalibrated uplift fails the build. Blocking is named, not ambient (v0.3.2), and the list is closed (v0.3.4): hard gates are validate, the [D] conformance job, ledger reconciliation and eval regression restricted to a frozen [D]-with-margin subset. Nothing else is blocking, and nothing blocking is allow_failure. The eval regression subset qualifies as a hard gate only because it is deterministic by construction: a frozen fixture set, a fixed threshold, and a margin wider than any run-to-run variance the harness can produce (greedy or temperature-0 decoding where the harness allows, otherwise a margin band established from measured variance). The remaining eval suite is [P], runs on the model-swap, harness-upgrade and quarterly cadence, emits typed events to the bus, and does not block per-merge. A [P] surface hard-blocking every MR would produce flaky red and is forbidden in the hard set. AETOS's CI job set includes three allow_failure jobs; inheriting the job set uncritically would inherit non-blocking gates wearing blocking colours. **Coverage gate (added v0.3.11).** Every test run reports two metrics — pass rate AND coverage — measured with `--cov-branch`, with **branch coverage the authoritative number** (a framework of fail-closed edges must prove its failure branches are exercised, not just its lines). 85% is a *reported, justify-below* target now — sub-85% requires a recorded justification but does not yet hard-fail — and **hardens into a C-2 hard gate** (a coverage job failing the build below threshold) once the harness runs. The post-implementation `quality` stage drives coverage as high as possible and surfaces branch gaps. Mechanism: the S-2 sandbox entrypoint runs pytest with `--cov=src/gleipnir --cov-branch --cov-report=term-missing`. See `.gleipnir/decisions/coverage-gate.md`.
- **C-3 Meta-tests.** Consistency (docs match code), delegation-discipline configuration, tool-contract stability. These verify the framework's own description of itself, closing the loop on "adversarial soundness is claimed, not verified" reflexively applied.
- **C-4 Runtime twin.** S-3 preflight executes the boundary subset of C-1 at every session start, so the guarantee holds between releases, not only at them. The boundary subset includes the closure-order check (G-1 terminal closure): the guarantee that the cage around in-framework agents is verified sound from outside before each session, not only at release.

---

## Part D: Decision register

| # | Decision | Status | Recommendation on record |
|---|---|---|---|
| D-1 | Runtime target | **Resolved (v0.3.9)** | opencode for v0.1, built against the S-1 contract so a pi.dev/pinion port is contract conformance, not rewrite. S-1 verified against the runtime: hooks 1-7 present (`tool.execute.before`/`after`, permission maps, `task` delegation, `question`, `experimental.session.compacting`, session-lifecycle events); hook 8 (platform-webhook ingress) absent, compensated by the E-2 persistent receiver, tracked not blocking |
| D-2 | Pipeline prose vs code | **Resolved: hybrid, elevated to G-5** | Deterministic engine calling the LLM per step; prose orchestration fails Axiom 2 |
| D-3 | v0.1 tool set | **Open** | T-1 (concept graph, decided direction with migration obligations), T-2, T-3, T-4 partial, T-5 minimal; **T-6 folded into S-2 and BUILT** (containerised build/test/lint sandbox, `bin/gleipnir-sandbox`; see `.gleipnir/decisions/s2-sandbox.md`) |
| D-4 | Trust-boundary substrate | **Resolved (v0.3.9), gates G-1/G-2/G-3** | B (container read-only mount) as Gleipnir-core; C (remote verifier) as the hardening tier for hosted/multi-agent operation; A (OS user + ACLs) as fallback where containers are unavailable. Config load path fixed mount-side/package-side; tree-side loading of anything enforcement-bearing is a G-1 fail |
| D-5 | Licence | **Open** | Decide before external publication |
| D-6 | Notional human rate for uplift | **Open** | Explicit versioned parameter, logged with every uplift figure |
| D-7 | Eval regression classification | **Resolved (v0.3.4)** | Frozen [D]-with-margin subset is a C-2 hard gate; higher-variance remainder is [P] on the model-swap, harness-upgrade and quarterly cadence, non-blocking per-merge |

D-1 and D-4 were the two load-bearing unknowns and were resolved together in the single substrate design pass (build-order step 1), recorded in `.gleipnir/plans/substrate-design-pass.md` and folded into this register at v0.3.9. The pass resolved the config load path (mount-side) and the S-2 boundary (option B core); the human-merge-block setting is not a Gleipnir decision. **Closure obligation named by the pass:** today's tree-side `.gleipnir/` is correct only for the authored-not-yet-closed step-0 phase; the enforcement-bearing subset (agent frontmatter/permission maps, guard code, the G-3 key, the rate table, weakening toggles) must relocate to the read-only mount at closure, while non-enforcement content (goals, plans, methodology skill text) may remain tree-side.

The T-1 memory architecture is a decided direction (concept graph), not an open decision: the SQLite read-path audit that once appeared to bear on it is an implementation fact, non-gating, and no D-8 is created.

The operator's escape hatch (`/plan`, `/build`) is not an open decision and not a vulnerability: it is out of scope by the Part 0 scope clause, the operator's agency to decline the framework, preserved by design.

### Open engineering seams (tracked, unspecified)

Distinct from the decision register: these are known gaps in the enforcement design, consciously deferred rather than resolved, recorded here so a freeze does not turn them from known-open into invisible-open. Each is in scope and none blocks the freeze of the architecture, but each is a specification debt against a later revision.

| # | Seam | Location | Note |
|---|---|---|---|
| E-1 | Broker argument policy | G-2, S-1.3.1 | The broker takes structured arguments but is not specified to refuse dangerous ones (force, protected-branch write, non-feature-branch push). Credential absence protects the credential, not the verb; a caller that speaks the broker's protocol still commands the action. The broker must enforce policy on its arguments, and the in-container IPC channel must not be reachable by arbitrary in-sandbox code, or G-2 holds only against credential theft, not against the action. |
| E-2 | Platform-webhook receiver has no component home | S-1.8, G-4b, Part T | The second bus ingress class requires a persistent inbound webhook receiver. T-4 is an outbound API client and T-5 an outbound notifier; neither listens, and a revert or unmerged close lands when no session is running, so an ephemeral opencode target cannot catch it. After the v0.3.4 scope correction these off-agent signals are the framework's chosen way of relating to human actions it does not control, which raises the cost of the missing component. It needs a T-row and an S-2 trust-class placement, and fits the Olympus persistent-instance direction. |
| E-3 | Novelty-triage signal quality | G-4c | Causal provenance identifies which turn a human corrected, not why. A redirect may be a caught failure, a scope change or a change of mind, and only the first is a guard candidate. The graduation-and-expiry criteria backstop this so it self-corrects, but the up-front signal is dirtier than "every human correction is a lesson". |
| E-4 | Build-order ranking versus G-3 body | Build order step 2, G-3 | Step 2 calls G-3.1 "highest value" while the G-3 body calls its blast radius local and names G-3.2 the larger gap. Read as best-win-per-cost the ranking is defensible, but as written the two statements contradict. |
| E-5 | Methodology amendments authored, bindings not built | K-2.1, `.gleipnir/skills/` | The GOTCHA/ATLAS inheritance-with-amendments contract (K-2.1) is authored and the step-0 scaffold realises it on paper, but the amendments reference bindings that do not exist yet: GOTCHA Amendment 1 points at the G-5 engine, Amendment 2 at the S-2 boundary, and the Guardrails link at G-4c graduation. Until those land, the amendments document the intended binding and resolve the layer-2 collision in prose, not in enforcement. The risk this seam tracks is a reader treating the amended skill text as an active guard rather than as authored-not-yet-closed content. |

C-1 already carries the conformance obligation that closes E-1 once specified (the adversarial-injection fixtures), and the T-1 memory-graph parity line. E-2 has no conformance obligation yet, by virtue of having no component. E-5 closes structurally as its three referenced bindings are built (G-5 in build-order step 3, S-2 in step 1, G-4c graduation in step 5); no separate conformance fixture is owed beyond those.

---

## Build order

1. **Substrate design pass** — **DONE (v0.3.9)**: D-1 plus D-4 plus S-1 contract verification, including the one evidence item it owns: the config load path (package versus tree), which flips G-1 between pass and fail for two surfaces. Agent reachability of merge is not an open item, it is closed by G-2 capability removal; the human's merge decision is out of scope. The load-bearing unknown, resolved once. Results in `.gleipnir/plans/substrate-design-pass.md` and Part D. Outcome: opencode runtime (S-1 7/8 hooks, #8 compensated); substrate option B core; config load path mount-side; terminal-closure + S-3 preflight mechanism specified.
 2. **G-3.1** keyed marker — **BUILT (v0.3.10 era)**: `src/gleipnir/verify/`, HMAC over tree/source hash, fail-closed, tested. G-3.2 is *not* buildable here: it depends on the G-5 engine to fetch attestations.
 3. **G-5** deterministic pipeline engine — **BUILT**: `src/gleipnir/engine/` (states, transitions, loop caps, human-question gate, attestation-only GATE edge), tested. The stage-to-role map (S-1.3.1) is authored. **Wire-in Python (bridge/allow-table/driver) built**; the structural pre/post-tool hook plugins (Tier-3 TS) remain to author so the engine actually gates delegations. **G-3.2** attestation edge exists in the engine; binding it to a real fetched attestation is the remaining integration.
 3a. **S-2 execution sandbox (T-6)** — **BUILT (v0.3.11)**: `bin/gleipnir-sandbox` + `Containerfile` + `src/gleipnir/sandbox/`; containerised build/test/lint with CRI detection, `--network=none`, ro source mount; `gleipnir-code` allowlist swapped to the sandbox entrypoint. This is the execution/blast-radius half of G-2 made real. Coverage gate wired into the entrypoint (C-2). See `.gleipnir/decisions/s2-sandbox.md`.
 4. **T layer minimal set**; completing the T-2 hardening delivers the *credential/broker* half of G-2, including the broker IPC separation, the single-holder grant (S-1.3.1), and the **E-1** argument policy.
5. **G-4** bus, ledger and observer: independent, can proceed in parallel from step 2; G-4c last, since it needs signal history.
6. **L distribution, K content, C harness wiring**: productionisation. C-1 fixtures accumulate from step 2 onward; C-2 gating switches on as soon as the harness runs green once.

**Closing invariant: guard closure is terminal and verified from outside.** Nothing an in-framework agent builds is trusted until the substrate is locked (S-2) and S-3 preflight confirms the lock from outside. This is the activation step, distinct from authoring: the guards may be written early, but they take effect against in-framework agents last, checked from outside. A guard whose closure is validated by the in-framework population it guards is the G-3 forgeable-evidence failure applied to activation, so closure and its verification originate outside the framework. The operator's ability to work outside the framework via the built-in `/plan` and `/build` agents is unaffected and preserved: closure cages in-framework agents, never the operator's escape hatch.

---

## Summary

Gleipnir is the twelve precepts plus six enforcement requirements on a specified substrate. G-1 makes the guards unreachable by the guarded; G-2 makes forbidden actions undisguisable by removing capability rather than inspecting use; G-3 makes verification evidence unforgeable; G-4 makes the ratchet's sense unblindable, gives it a channel for genuine novelty and instruments it with an honest economic ledger; G-5 moves sequencing itself out of the probabilistic layer, converting the gates that AETOS could only admonish into states that cannot be skipped; G-6 makes persistent memory unpoisonable, treating it as untrusted tiered input with named writers, provenance, review-gated promotion and digest-verified integrity so influence cannot survive across sessions. The roster on which all six stand is inherited-and-audited from AETOS v4, with only the engine's stage-to-role binding authored net-new (S-1.3.1). The methodology on which planning stands (GOTCHA and ATLAS) is likewise inherited-and-amended, with a single collision at layer 2 (Orchestration) resolved in favour of G-5 (K-2.1). Material design decisions do not hide in the planner: a dedicated `gleipnir-brainstorm` role surfaces them to the operator through a Propose→Converge gate (precept 10) backed by the K-3 decision-frameworks + bias-detector catalogue, and `gleipnir-plan` plans only from the converged brief. Gleipnir binds agents acting within it and preserves the operator's freedom to decline it: the built-in `/plan` and `/build` agents are the sanctioned escape hatch, out of scope by design, and the guards are locked from outside as the terminal act, verified by S-3 preflight before each session, against in-framework agents only. Every enforcement requirement traces to the second axiom: a guard must not be reachable, forgeable, or blindable by the population it guards — nor poisonable by text that persists beyond the session (G-6).
